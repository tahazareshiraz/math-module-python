from setuptools import setup, find_packages

setup(
    name="taha-math-shiraz",
    version="1.0.0",
    author="Taha",
    description="A pure-python extended math library, faster and richer than the standard math module",
    packages=find_packages(),
    python_requires=">=3.6",
)
